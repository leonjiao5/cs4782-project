# ── Python / editor artifacts ──────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
.env
.DS_Store

# ── Model weights ──────────────────────────────────────────────────────────
wandb/
outputs/
checkpoints/
*.pt
*.bin
*.safetensors

# ── HuggingFace cache ──────────────────────────────────────────────────────
hf_cache/

# ── SLURM scripts and logs (project runs from Colab, not HPC) ──────────────
slurm/

# ── Heavy SLURM results (renamed from results/) ────────────────────────────
results_slurm/

# ── Root-level old analysis figures (not a required deliverable folder) ─────
/figures/

# ── Runtime config generated by apply_config() in notebook ─────────────────
code/configs/run_config.yaml

# ── Specific generated log files ───────────────────────────────────────────
results/logs/leakage_drops.jsonl

# ── Allow .gitkeep placeholders ────────────────────────────────────────────
!**/.gitkeep
